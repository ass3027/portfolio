# 이력서 / 개인정보 문서 (버전 관리 제외)
*.pdf

# 병렬 작업용 git worktree 디렉터리 (버전 관리 제외)
.claude/worktrees/

# Claude Code 로컬 설정 (버전 관리 제외)
.claude/settings.local.json

# Playwright MCP 작업 산출물(PDF 검증 스크린샷 등)
.playwright-mcp/

# 루트에 떨어지는 PDF/화면 검증 스크린샷 (하위 폴더의 다이어그램 자산은 제외 안 함)
/*.png

# 로컬 작업 메모 (할 일은 GitHub Issues로 관리)
/todo.md
